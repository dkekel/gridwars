description = 'GridWars - Root Project'

subprojects {
    // Defining some common properties that are shared by the sub-projects
    ext {
        gridwarsGroup = 'cern.ais.gridwars'

        gridwarsCoreVersion = '2.0.0-SNAPSHOT'
        gridwarsWebVersion = '1.0.0-SNAPSHOT'

        javaVersion = 1.8

        springBootVersion = '2.0.1.RELEASE'
    }
}
