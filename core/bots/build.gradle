apply plugin: 'application'

mainClassName = 'cern.ais.gridwars.bot.App'

jar {
    manifest {
        attributes(
		        'Bot-Class-Name': 'cern.ais.gridwars.bot.admin.Level3AdminBot'
        )
    }
}

dependencies {
	compile project(':core:emulator')
    compile fileTree(dir: 'lib')
}
